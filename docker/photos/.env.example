# docker/photos/.env.example
# Immich photo stack. UPLOAD_LOCATION must exist on the NAS.
TZ=Europe/Prague
PHOTOS_ROOT=/srv/photos
UPLOAD_LOCATION=${PHOTOS_ROOT}/immich
DB_DATA_LOCATION=${PHOTOS_ROOT}/postgres
DB_PASSWORD=change-me